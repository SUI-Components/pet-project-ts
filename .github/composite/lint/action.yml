
name: 'Lint monorepo'
description: 'Lint monorepo using sui-lint'

runs:
  using: composite

  steps:
      - name: Install
        uses: scmspain/platform-common--actions/frontend/install@v0

      - uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'

      - name: Lint
        shell: bash
        run: echo "NPM RUN LINT FAIL!!"
